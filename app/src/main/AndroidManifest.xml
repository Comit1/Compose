<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comit.compose">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ComposeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Compose">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Compose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".chart.ChartActivity" />
        <activity android:name=".chart.LineChartActivity" />
        <activity android:name=".chart.BarChartActivity" />
        <activity android:name=".chart.PieChartActivity" />

        <activity android:name=".navigation.NavigationActivity" />
        <activity android:name=".navigation.NormalNavigationActivity" />
        <activity android:name=".navigation.BottomNavigationActivity" />
        <activity android:name=".navigation.TopNavigationActivity" />
        <activity android:name=".navigation.BannerActivity" />

        <activity android:name=".lifecycle.LifecycleActivity" />

        <activity android:name=".layout.LayoutActivity" />

        <activity android:name=".dialog.DialogActivity" />

    </application>

</manifest>